export interface ColorsProps {
  foreground: {
    primary: string;
    secondary: string;
    light: string;
  };
  background: {
    primary: string;
    secondary: string;
    accent: string;
    darkBlue: string;
    lightPurple: string;
    lightBlue: string;
    light: string;
  };
}

export interface FontsProps {
  heading: string;
  body: string;
}
